"""Imperative shell for the trial scoring path (spec P2).

Wiring, artifact loading, caching, and orchestration. Pure scoring
logic lives in core/ — this package moves bytes and passes frozen
parameter objects down as arguments.
"""
