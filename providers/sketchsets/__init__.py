"""Sketch-pair dataset adapters behind the SketchPairSource protocol.

Spec: docs/specs/scoring-path.md section 8. Code in other directories
does not know which dataset is wired — configuration selects the
adapter, and the SketchPair rules hold at each adapter's boundary.
"""
