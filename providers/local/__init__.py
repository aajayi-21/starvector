"""Local model providers.

The torch stack installs with the local dependency group, thus the
default environment and the test suite stay free of it. Spec:
docs/specs/pool-preparation.md section 8 (U2).
"""
