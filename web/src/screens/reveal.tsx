/** The reveal screen (mock 1c) — built out in C8. */

export function RevealScreen(): React.JSX.Element {
  return (
    <div style={{ padding: 28 }}>
      <div className="card">
        <span className="card-kicker">Reveal</span>
        <p className="text-muted">The score report lands in C8.</p>
      </div>
    </div>
  );
}
