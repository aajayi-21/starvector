/** The daily trial screen (mock 1a/1f) — built out in C6. */

export function TodayScreen(): React.JSX.Element {
  return (
    <div style={{ padding: 28 }}>
      <div className="card">
        <span className="card-kicker">Today's target</span>
        <p className="text-muted">The trial workspace lands in C6.</p>
      </div>
    </div>
  );
}
